figure out where cost stuff lives and is shown in agent mode

make it generic beyond Anthropic
work with local llm

max tokens and temp parameters from cli are not integrated
separate tools module, not just in cli
